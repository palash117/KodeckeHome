import React from "react";

function DocScannerPolicyContents() {
  return (
    <div style={{ textAlign: "center" }}>
      This app does not collect your data outsde the user's mobile. All the data
      submitted by the user is kept within the device and will never be
      collected.
    </div>
  );
}

export default DocScannerPolicyContents;
